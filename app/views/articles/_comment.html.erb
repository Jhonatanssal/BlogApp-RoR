<div>
  <h4>Comment by</h4>
  <h4 class='text-primary'><%= comment.author_name %></h4>
  <p class="comment pl-3">
    <%= comment.body %>
  </p>
  <p class="text-black-50">
  Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later
  </p>
</div>