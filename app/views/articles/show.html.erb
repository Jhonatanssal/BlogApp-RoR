<h1><%= @article.title %></h1>
<p class="p-3 bg-light mt-4"><%= @article.body %></p>
<br><br>
<%= link_to 'Edit', edit_article_path(@article), class: 'btn btn-primary' %>
<%= link_to 'Delete', article_path(@article), method: :delete,
data: { confirm: 'Are you sure you want to DELETE this article?' }, class: 'btn btn-danger' %><br><br>
<%= link_to 'Back to Articles list', articles_path, class: 'btn btn-secondary' %>